/**
 * @description
 * Produce deterministic paths for each value in object/array or itself for another json-like instances.
 * The dictionary itself should be also deterministic array.
 */
export function json_to_path_dictionary() {
}
